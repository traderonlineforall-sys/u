# Step 4 - Central Nickname Identity

الهدف من المرحلة دي: الكنية تبقى مركزية وثابتة للمستخدم بدل ما الدعم أو الاقتراحات يطلبوا أسماء منفصلة.

## ما الذي يتغير؟

- المستخدم يكتب كنية خيالية عند تسجيل الدخول أول مرة فقط.
- مثال واضح يظهر في شاشة الدخول: `عقرب الصحراء`.
- لا يُطلب الاسم عند فتح مربع الدعم.
- نفس الكنية تظهر في الدعم، الاقتراحات، الردود، وقائمة المتصلين.
- إذا الأدمن عمل `Reset nickname`، نفس المستخدم يظل بنفس `user_id` لكن يطلب منه كنية جديدة عند الدخول القادم.
- السيرفر هو الذي يعتمد الكنية من جدول `support_users`؛ المتصفح لا يستطيع تغيير الاسم بسهولة بمجرد تعديل localStorage.

## أقل استهلاك Cloudflare

هذا التحديث لا يضيف polling ولا refresh مستمر. التحقق من الكنية يتم داخل طلب تسجيل الدخول نفسه، والكتابة الحالية تمر بالفعل عبر API كما في Step 2.

## ترتيب التطبيق

1. ارفع ملف GitHub Action الخاص بـ Step 4.
2. انتظر GitHub Action حتى ينجح.
3. انتظر Cloudflare Deploy حتى ينجح.
4. جرّب تسجيل الدخول من متصفح عادي.
5. بعد نجاح الكود، شغّل SQL التالي في Supabase:

```text
SUPABASE_NICKNAME_IDENTITY_STEP4.sql
```

## الاختبار بعد SQL

- افتح التول من نافذة جديدة.
- لو مفيش كنية محفوظة، سيظهر حقل الكنية في صفحة الدخول.
- اكتب كنية خيالية، مثل: `عقرب الصحراء`.
- افتح الدعم: لا يجب أن يطلب اسم.
- أرسل رسالة دعم: يجب أن تظهر الكنية.
- أضف اقتراح ورد على اقتراح: يجب أن تظهر نفس الكنية.
- افتح الأدمن ثم Support ثم User Names، واضغط `Reset nickname` لمستخدم تجريبي.
- عند دخول نفس المستخدم مرة أخرى، يجب أن يطلب كنية جديدة.

## الرجوع للطوارئ

لو حصلت مشكلة بعد SQL، شغّل:

```text
SUPABASE_NICKNAME_IDENTITY_STEP4_ROLLBACK.sql
```

ولو المشكلة من الكود نفسه، ارجع آخر commit من GitHub أو اعمل Revert للـ commit الذي أنشأه GitHub Action.
